# nasdash v2.0.5

## 更新内容

| 改动点 | 说明 / 效果 |
|---|---|
| 新增 C 档 · 只读表面扫描 | 像硬盘哨兵那样<b>只读</b>扫盘面，不写数据、不伤盘，<b>所有盘都能跑</b>——包括正在用的盘/阵列成员（B 档破坏性扫不了的盘也能体检） |
| 扇区扫描网格可视化 | 扫描时显示 80×8 扇区地图：灰=未扫、绿=正常、红=坏块、黄=当前扫描位置，进度实时填充（仿硬盘哨兵） |
| 扫描面板最小化 | 扫描进行中可一键收起成一行小条（盘名 + 进度），后台继续扫，随时展开 |
| 硬盘自检入口合并 | A/B/C 三个按钮合并成每块盘一个「硬盘自检」入口，点开按盘的情况给可选方式（在用盘不显示清空式扫描），界面更清爽 |
| 修复坏块扫描进度卡 0% | badblocks 用退格符刷新进度导致进度解析卡在 0.00%，改为取最新进度值（B 档 4 遍扫描同样修复） |
| 温度读数修准 | CPU 温度不再偏高约 10°C（修正读取字段），检测/温度/风扇三页显示完全一致 |
| CPU 温度平滑显示 | 温度不再瞬间跳变（39↔47 那种），改为平滑过渡；全部温度取整显示（无小数） |
| 温度监控页实时刷新 | 温度墙改为每 5 秒就地刷新，数字实时变化（此前 30 秒整页刷新才更新） |
| 风扇页防闪 | 风扇页不再整页闪动——转速/温度走 1s/5s 轻量就地更新，30 秒自动刷新豁免风扇页 |
| 硬盘健康告警归位 | 硬盘 SMART 告警只在硬盘页内显示，不再顶到全局横幅（避免误以为整机异常） |
| 温度卡片分类 | 温度 chip 按 核心/NVMe/SSD/机械 分组展示 + 统一高度对齐 |
| 数据刷新提速 | 实时接口毫秒级返回（此前部分接口 2 秒+）；「立即刷新」可强制重新扫描（不被缓存挡） |

## 升级说明
- 覆盖安装即可，原配置（风扇标签、温控设置等）全部保留。
- 若此前通过飞牛应用中心安装，直接在应用中心更新；卸载时勾选「保留配置」即可。
