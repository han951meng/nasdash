# nasdash v2.1.0

飞牛OS 第三方 NAS 硬件监控面板。本次更新重点完善显卡监控、实时刷新和温度监控页面显示。

## 更新内容

| 改动点 | 说明 / 效果 |
|---|---|
| 系统资源页新增 GPU 占用 | 顶部系统资源卡新增「GPU 占用」，位置为 CPU 后、网络前；优先显示独显，没有独显时显示核显。 |
| 多显卡显示更清晰 | 检测到核显 + 一张或多张独显时，顶部只显示主显卡，详细显卡卡片通过下拉框切换查看，避免多张显卡同时堆在页面上。 |
| 核显占用率修复 | 核显不再把整机内存占用率误显示成 GPU 占用；空闲核显可正确显示 0%，与飞牛系统资源页口径一致。 |
| 核显显存显示修复 | 核显优先显示实际图形缓冲占用，并标记共享内存口径；不再直接把整机内存占用率当成显存占用。 |
| 实时数据恢复秒级刷新 | 系统资源页的 CPU、内存、网络、GPU 等轻量数据改为每秒刷新，直接读取后台已有的实时指标缓存，不重复执行硬件全量检测。 |
| 修复实时数据卡顿 | 修复核显监测异常导致实时数据更新变慢的问题，页面数据恢复正常跳动。 |
| 修复温度监控文字重叠与偏移 | 论坛用户反馈：显卡测点名称过长时，独显 / 核显文字会撑破温度墙格子，遮挡相邻卡片；部分卡片文字看起来也没有居中。现已限制格子宽度、自动换行，并让名称与温度值在卡片内水平/垂直居中；「★最准」徽标保持整行显示，不再被拆开。 |

## 升级说明

- 覆盖安装即可，原有风扇标签、温控设置、历史数据等配置会保留。
- 如果通过飞牛应用中心安装，直接上传新版 fpk 更新即可。
- 多显卡用户进入「系统资源」页后，可在显卡卡片右上角下拉选择要查看的显卡。
- 顶部 GPU 占用只代表当前主显卡；要分别查看每张显卡，请使用显卡卡片下拉框。

## 本次验证

- 已在飞牛 NAS 真机安装 v2.1.0 并完成用户验收。
- 系统资源页实时数据按秒级更新，CPU、网络等数值可持续变化。
- 核显空闲时 GPU 占用显示 0%，与飞牛系统资源页一致。
- 温度墙长名称、文字居中和「★最准」徽标显示已检查通过。
