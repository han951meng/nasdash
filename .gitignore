# 本地打包/构建暂存目录（非源码）
_app/
_rebuild/

# Python 缓存
__pycache__/

# 本地自测虚拟环境
.venv/

# 运行时日志
debug.log

# 本地打包中间产物（app.tgz 由打包脚本重新生成，不入库）
app.tgz

# 构建产物 fpk（仅作 GitHub Release 资产，不入库；由 build.sh 重新生成）
nasdash.fpk

# 论坛发帖草稿等杂物（非源码）
forum_post_*
forum_reply_*
